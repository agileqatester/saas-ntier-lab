[pytest]
testpaths = .
pythonpath = .
markers =
    smoke: environment readiness
    isolation: tenant isolation contracts
    requires_cluster: needs kubectl + deployed tenants
filterwarnings =
    ignore::urllib3.exceptions.InsecureRequestWarning
